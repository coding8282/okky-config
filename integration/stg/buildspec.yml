version: 0.2

phases:
  build:
    commands:
      - mvn clean install

artifacts:
  files:
    - integration/stg/appspec.yml
    - integration/stg/scripts/*
    - target/okky-config-1.0.0.jar
  discard-paths: yes

cache:
  paths:
    - '/root/.m2/**/*'